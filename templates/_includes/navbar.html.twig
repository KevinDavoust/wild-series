<nav class="navbar navbar-expand-lg navbar-light">
    <div class="d-flex align-items-center h-100 w-100 justify-content-between">
        <a class="navbar-brand wsnavli" href="/">Wild Series</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
            <ul class="navbar-nav d-flex justify-content-between">
                <li class="nav-item ">
                    {{ component('navbar') }}
                </li>
            </ul>
        <ul class="navbar-nav d-flex justify-content-between">
                <li class="nav-item">
                    <a class="nav-link wsnavli" href="/program">Program</a>
                </li>
        </ul>
        <ul class="navbar-nav d-flex justify-content-between">
                <li class="nav-item">
                    <a class="nav-link wsnavli" href="/season">Season</a>
                </li>
        </ul>
        <ul class="navbar-nav d-flex justify-content-between me-5">
                <li class="nav-item">
                    <a class="nav-link wsnavli" href="/episode">Episode</a>
                </li>
        </ul>
        <ul class="navbar-nav d-flex justify-content-between me-5">
            <li class="nav-item">
                <a class="nav-link wsnavli" href="/actor">Actor</a>
            </li>
        </ul>

        {% if app.user %}
            {{ app.user.email }}
            <a href="{{ path('logout') }}"> Se déconnecter</a>
        {% else %}
            <a href="{{ path('login') }}"> Se connecter</a>
        {% endif %}

        <button data-action="theme#toggleDarkMode">Dark mode</button>
        {#
        PAREIL QUE
        <button {{ stimulus_action('theme', 'toggleDarkMode') }}>Toggle dark mode</button>
        #}

    </div>
</nav>