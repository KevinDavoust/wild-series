<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
            <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-KK94CHFLLe+nY2dmCWGMq91rCGa5gtU4mk92HdvYe+M/SXH301p5ILy+dN9+nJOZ" crossorigin="anonymous">
            {#<link rel="stylesheet" href="/styles/app.css">#}

        {% endblock %}


    </head>

    <body {{ stimulus_controller('theme symfony/ux-swup/swup', {
        theme: 'slide',
        animateHistoryBrowsing: true,
    }) }}
            {{ stimulus_target('theme', 'body') }}>



    {% block navbar %}
        {% include '_includes/navbar.html.twig' %}
    {% endblock %}
    {% block flashbag %}
        {% for category, messages in app.flashes %}
             {% for message in messages %}
                <div class="alert alert-{{ category }}">
                    {{ message }}
                </div>
            {% endfor %}
    {% endfor %}
    {% endblock %}

    <div id="swup">

        {% block body %}

        {% endblock %}
        </div>


    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
        {#<script type="text/javascript" src="script.js"></script>#}
        {#<script>
            window.addEventListener('click', function() {
                var h1Tags = document.querySelectorAll('h1'); // Sélectionne toutes les balises h1

                for (var i = 0; i < h1Tags.length; i++) {
                    var randomColor = getRandomColor(); // Génère une couleur aléatoire
                    h1Tags[i].style.color = randomColor; // Applique la couleur aléatoire à la balise h1
                }

                function getRandomColor() {
                    var letters = '0123456789ABCDEF';
                    var color = '#';
                    for (var i = 0; i < 6; i++) {
                        color += letters[Math.floor(Math.random() * 16)];
                    }
                    return color;
                }
            });



            window.addEventListener('click', function() {
                var bodyTags = document.getElementsByTagName('body'); // Sélectionne toutes les balises h1

                for (var i = 0; i < bodyTags.length; i++) {
                    var randomColor = getRandomColor(); // Génère une couleur aléatoire
                    bodyTags[i].style.backgroundColor = randomColor; // Applique la couleur aléatoire à la balise h1
                }

                function getRandomColor() {
                    var letters = '0123456789ABCDEF';
                    var color = '#';
                    for (var i = 0; i < 6; i++) {
                        color += letters[Math.floor(Math.random() * 16)];
                    }
                    return color;
                }
            });

            window.addEventListener('click', function() {
                var h2Tags = document.getElementsByTagName('h2'); // Sélectionne toutes les balises h1

                for (var i = 0; i < h2Tags.length; i++) {
                    var randomColor = getRandomColor(); // Génère une couleur aléatoire
                    h2Tags[i].style.color = randomColor; // Applique la couleur aléatoire à la balise h1
                }

                function getRandomColor() {
                    var letters = '0123456789ABCDEF';
                    var color = '#';
                    for (var i = 0; i < 6; i++) {
                        color += letters[Math.floor(Math.random() * 16)];
                    }
                    return color;
                }
            });

            window.addEventListener('click', function() {
                var pTags = document.getElementsByTagName('p'); // Sélectionne toutes les balises h1

                for (var i = 0; i < pTags.length; i++) {
                    var randomColor = getRandomColor(); // Génère une couleur aléatoire
                    pTags[i].style.color = randomColor; // Applique la couleur aléatoire à la balise h1
                }

                function getRandomColor() {
                    var letters = '0123456789ABCDEF';
                    var color = '#';
                    for (var i = 0; i < 6; i++) {
                        color += letters[Math.floor(Math.random() * 16)];
                    }
                    return color;
                }
            });

            window.addEventListener('click', function() {
                var aTags = document.getElementsByTagName('a'); // Sélectionne toutes les balises h1

                for (var i = 0; i < aTags.length; i++) {
                    var randomColor = getRandomColor(); // Génère une couleur aléatoire
                    aTags[i].style.color = randomColor; // Applique la couleur aléatoire à la balise h1
                }

                function getRandomColor() {
                    var letters = '0123456789ABCDEF';
                    var color = '#';
                    for (var i = 0; i < 6; i++) {
                        color += letters[Math.floor(Math.random() * 16)];
                    }
                    return color;
                }
            });

            window.addEventListener('click', function() {
                var navTags = document.getElementsByTagName('nav'); // Sélectionne toutes les balises h1

                for (var i = 0; i < navTags.length; i++) {
                    var randomColor = getRandomColor(); // Génère une couleur aléatoire
                    navTags[i].style.backgroundColor = randomColor; // Applique la couleur aléatoire à la balise h1
                }

                function getRandomColor() {
                    var letters = '0123456789ABCDEF';
                    var color = '#';
                    for (var i = 0; i < 6; i++) {
                        color += letters[Math.floor(Math.random() * 16)];
                    }
                    return color;
                }
            });

            window.addEventListener('DOMContentLoaded', function() {
                var h1Tags = document.querySelectorAll('h1'); // Sélectionne toutes les balises h1

                for (var i = 0; i < h1Tags.length; i++) {
                    var randomColor = getRandomColor(); // Génère une couleur aléatoire
                    h1Tags[i].style.color = randomColor; // Applique la couleur aléatoire à la balise h1
                }

                function getRandomColor() {
                    var letters = '0123456789ABCDEF';
                    var color = '#';
                    for (var i = 0; i < 6; i++) {
                        color += letters[Math.floor(Math.random() * 16)];
                    }
                    return color;
                }
            });

            window.addEventListener('', function() {
                alert('dom chargé')
            })

            window.addEventListener('DOMContentLoaded', function() {
                var bodyTags = document.getElementsByTagName('body'); // Sélectionne toutes les balises h1

                for (var i = 0; i < bodyTags.length; i++) {
                    var randomColor = getRandomColor(); // Génère une couleur aléatoire
                    bodyTags[i].style.backgroundColor = randomColor; // Applique la couleur aléatoire à la balise h1
                }

                function getRandomColor() {
                    var letters = '0123456789ABCDEF';
                    var color = '#';
                    for (var i = 0; i < 6; i++) {
                        color += letters[Math.floor(Math.random() * 16)];
                    }
                    return color;
                }
            });

            window.addEventListener('DOMContentLoaded', function() {
                var h2Tags = document.getElementsByTagName('h2'); // Sélectionne toutes les balises h1

                for (var i = 0; i < h2Tags.length; i++) {
                    var randomColor = getRandomColor(); // Génère une couleur aléatoire
                    h2Tags[i].style.color = randomColor; // Applique la couleur aléatoire à la balise h1
                }

                function getRandomColor() {
                    var letters = '0123456789ABCDEF';
                    var color = '#';
                    for (var i = 0; i < 6; i++) {
                        color += letters[Math.floor(Math.random() * 16)];
                    }
                    return color;
                }
            });

            window.addEventListener('DOMContentLoaded', function() {
                var pTags = document.getElementsByTagName('p'); // Sélectionne toutes les balises h1

                for (var i = 0; i < pTags.length; i++) {
                    var randomColor = getRandomColor(); // Génère une couleur aléatoire
                    pTags[i].style.color = randomColor; // Applique la couleur aléatoire à la balise h1
                }

                function getRandomColor() {
                    var letters = '0123456789ABCDEF';
                    var color = '#';
                    for (var i = 0; i < 6; i++) {
                        color += letters[Math.floor(Math.random() * 16)];
                    }
                    return color;
                }
            });

            window.addEventListener('DOMContentLoaded', function() {
                var aTags = document.getElementsByTagName('a'); // Sélectionne toutes les balises h1

                for (var i = 0; i < aTags.length; i++) {
                    var randomColor = getRandomColor(); // Génère une couleur aléatoire
                    aTags[i].style.color = randomColor; // Applique la couleur aléatoire à la balise h1
                }

                function getRandomColor() {
                    var letters = '0123456789ABCDEF';
                    var color = '#';
                    for (var i = 0; i < 6; i++) {
                        color += letters[Math.floor(Math.random() * 16)];
                    }
                    return color;
                }
            });

            window.addEventListener('DOMContentLoaded', function() {
                var navTags = document.getElementsByTagName('nav'); // Sélectionne toutes les balises h1

                for (var i = 0; i < navTags.length; i++) {
                    var randomColor = getRandomColor(); // Génère une couleur aléatoire
                    navTags[i].style.backgroundColor = randomColor; // Applique la couleur aléatoire à la balise h1
                }

                function getRandomColor() {
                    var letters = '0123456789ABCDEF';
                    var color = '#';
                    for (var i = 0; i < 6; i++) {
                        color += letters[Math.floor(Math.random() * 16)];
                    }
                    return color;
                }
            });

            window.addEventListener('click', function() {
                var h1Tags = document.querySelectorAll('h1'); // Sélectionne toutes les balises h1

                for (var i = 0; i < h1Tags.length; i++) {
                    var randomSize = getRandomSize(); // Génère une taille de police aléatoire
                    h1Tags[i].style.fontSize = randomSize + 'px'; // Applique la taille de police aléatoire à la balise h1
                }

                function getRandomSize() {
                    var sizes = [16, 24, 32, 40, 48, 56, 64, 72, 80, 88, 96];

                    var randomSize = sizes[Math.floor(Math.random() * sizes.length)];
                    return randomSize;
                }
            });

            window.addEventListener('click', function() {
                var h2Tags = document.querySelectorAll('h2'); // Sélectionne toutes les balises h1

                for (var i = 0; i < h2Tags.length; i++) {
                    var randomSize = getRandomSize(); // Génère une taille de police aléatoire
                    h2Tags[i].style.fontSize = randomSize + 'px'; // Applique la taille de police aléatoire à la balise h1
                }

                function getRandomSize() {
                    var sizes = [16, 24, 32, 40, 48, 56, 64, 72, 80, 88, 96];

                    var randomSize = sizes[Math.floor(Math.random() * sizes.length)];
                    return randomSize;
                }
            });

            window.addEventListener('click', function() {
                var pTags = document.querySelectorAll('p'); // Sélectionne toutes les balises h1

                for (var i = 0; i < pTags.length; i++) {
                    var randomSize = getRandomSize(); // Génère une taille de police aléatoire
                    pTags[i].style.fontSize = randomSize + 'px'; // Applique la taille de police aléatoire à la balise h1
                }

                function getRandomSize() {
                    var sizes = [16, 24, 32, 40, 48, 56, 64, 72, 80, 88, 96];

                    var randomSize = sizes[Math.floor(Math.random() * sizes.length)];
                    return randomSize;
                }
            });

            window.addEventListener('click', function() {
                var aTags = document.querySelectorAll('a'); // Sélectionne toutes les balises h1

                for (var i = 0; i < aTags.length; i++) {
                    var randomSize = getRandomSize(); // Génère une taille de police aléatoire
                    aTags[i].style.fontSize = randomSize + 'px'; // Applique la taille de police aléatoire à la balise h1
                }

                function getRandomSize() {
                    var sizes = [16, 24, 32, 40, 48, 56, 64, 72, 80, 88, 96];

                    var randomSize = sizes[Math.floor(Math.random() * sizes.length)];
                    return randomSize;
                }
            });


        </script>#}
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ENjdO4Dr2bkBIFxQpeoTz1HIcje39Wm4jDKdf19U8gI4ddQ3GYNS7NTKfAdVQSZe" crossorigin="anonymous"></script>

    {% endblock %}

    </body>

</html>
